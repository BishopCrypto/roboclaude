import React from 'react';

const Player = ({ player, renderPlayer }) => {
  // Player rendering is handled by the renderPlayer function passed from Game
  return null; // This component doesn't render anything directly
};

export default Player;
