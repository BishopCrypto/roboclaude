import React from 'react';

const Bullet = ({ bullet, renderBullet }) => {
  // Bullet rendering is handled by the renderBullet function passed from Game
  return null; // This component doesn't render anything directly
};

export default Bullet;
